From: gh123man <gh123man@users.noreply.github.com>
Subject: [PATCH] scsi: ufs: recover clock-gating hibern8-enter failures inline

ufshcd_gate_work() restores the software clock-gating state when
DME_HIBER_ENTER fails but does not repair the link. Recover inline for the
SM8550 path and disable runtime clock gating after the first failure.

Linux 7.2 uses a plain hard IRQ handler, so the former relink completion
poller is no longer needed. Keep a dedicated flag while inline recovery owns
UIC errors so the generic error handler cannot race a second reset.

Ported from the armada-packages sm8550-sleep stack (ROCKNIX PR 3126), with
one adaptation: the archived version keyed off the sm8550.ns=1 cmdline gate
(patch 1029), which this tree does not carry. Here hba->sm8550_native_sleep_
workarounds is defined and set from the qcom,sm8550-ufshc compatible in
ufs_qcom_init(), keeping the workaround scoped to SM8550 hardware without
a boot parameter.

Signed-off-by: gh123man <gh123man@users.noreply.github.com>
Signed-off-by: drewano <drewano@users.noreply.github.com>

diff --git a/drivers/ufs/core/ufshcd.c b/drivers/ufs/core/ufshcd.c
index 7d9c72378..624424fa6 100644
--- a/drivers/ufs/core/ufshcd.c
+++ b/drivers/ufs/core/ufshcd.c
@@ -2030,6 +2030,7 @@ static void ufshcd_gate_work(struct work_struct *work)
 {
 	struct ufs_hba *hba = container_of(work, struct ufs_hba,
 			clk_gating.gate_work.work);
+	bool recover_inline = hba->sm8550_native_sleep_workarounds;
 	int ret;
 
 	scoped_guard(spinlock_irqsave, &hba->clk_gating.lock) {
@@ -2059,16 +2060,45 @@ static void ufshcd_gate_work(struct work_struct *work)
 
 	/* put the link into hibern8 mode before turning off clocks */
 	if (ufshcd_can_hibern8_during_gating(hba)) {
+		if (recover_inline)
+			WRITE_ONCE(hba->sm8550_gate_recovery_active, true);
 		ret = ufshcd_uic_hibern8_enter(hba);
 		if (ret) {
 			hba->clk_gating.state = CLKS_ON;
-			dev_err(hba->dev, "%s: hibern8 enter failed %d\n",
-					__func__, ret);
 			trace_ufshcd_clk_gating(hba,
 						hba->clk_gating.state);
+			if (!recover_inline) {
+				dev_err(hba->dev, "%s: hibern8 enter failed %d\n",
+						__func__, ret);
+				return;
+			}
+
+			dev_err(hba->dev,
+				"%s: hibern8 enter failed %d, recovering link\n",
+				__func__, ret);
+			ret = ufshcd_link_recovery(hba);
+
+			scoped_guard(spinlock_irqsave, &hba->clk_gating.lock) {
+				if (hba->clk_gating.is_enabled) {
+					hba->clk_gating.active_reqs++;
+					hba->clk_gating.is_enabled = false;
+				}
+			}
+			WRITE_ONCE(hba->sm8550_gate_recovery_active, false);
+
+			if (ret)
+				dev_err(hba->dev,
+					"%s: link recovery failed %d\n",
+					__func__, ret);
+			else
+				dev_warn(hba->dev,
+					 "%s: link recovered; runtime clock gating disabled\n",
+					 __func__);
 			return;
 		}
 		ufshcd_set_link_hibern8(hba);
+		if (recover_inline)
+			WRITE_ONCE(hba->sm8550_gate_recovery_active, false);
 	}
 
 	ufshcd_disable_irq(hba);
@@ -4483,7 +4513,8 @@ static int ufshcd_uic_pwr_ctrl(struct ufs_hba *hba, struct uic_command *cmd)
 	spin_lock_irqsave(hba->host->host_lock, flags);
 	hba->active_uic_cmd = NULL;
 	hba->uic_async_done = NULL;
-	if (ret && !hba->pm_op_in_progress) {
+	if (ret && !hba->pm_op_in_progress &&
+	    !READ_ONCE(hba->sm8550_gate_recovery_active)) {
 		ufshcd_set_link_broken(hba);
 		ufshcd_schedule_eh_work(hba);
 	}
diff --git a/drivers/ufs/host/ufs-qcom.c b/drivers/ufs/host/ufs-qcom.c
index 26d42e432..871635aa1 100644
--- a/drivers/ufs/host/ufs-qcom.c
+++ b/drivers/ufs/host/ufs-qcom.c
@@ -1617,6 +1617,10 @@ static int ufs_qcom_init(struct ufs_hba *hba)
 	host->hba = hba;
 	ufshcd_set_variant(hba, host);
 
+	hba->sm8550_native_sleep_workarounds =
+		drvdata && of_device_is_compatible(dev->of_node,
+						   "qcom,sm8550-ufshc");
+
 	/* Setup the optional reset control of HCI */
 	host->core_reset = devm_reset_control_get_optional(hba->dev, "rst");
 	if (IS_ERR(host->core_reset)) {
diff --git a/include/ufs/ufshcd.h b/include/ufs/ufshcd.h
index 609f0452c..0d0bb1358 100644
--- a/include/ufs/ufshcd.h
+++ b/include/ufs/ufshcd.h
@@ -1058,6 +1058,8 @@ enum ufshcd_mcq_opr {
  *	ufshcd_resume_complete()
  * @complete_clkgate_hold: whether ufshcd_resume_complete() must release
  *	clock-gating hold acquired by ufshcd_suspend_prepare()
+ * @sm8550_native_sleep_workarounds: SM8550-only vendor sleep workaround path
+ * @sm8550_gate_recovery_active: clock-gate Hibern8 recovery owns UIC errors
  * @mcq_sup: is mcq supported by UFSHC
  * @mcq_enabled: is mcq ready to accept requests
  * @mcq_esi_enabled: is mcq ESI configured
@@ -1240,6 +1242,8 @@ struct ufs_hba {
 	unsigned int nr_queues[HCTX_MAX_TYPES];
 	bool complete_put;
 	bool complete_clkgate_hold;
+	bool sm8550_native_sleep_workarounds;
+	bool sm8550_gate_recovery_active;
 	bool scsi_host_added;
 	bool mcq_sup;
 	bool lsdb_sup;
