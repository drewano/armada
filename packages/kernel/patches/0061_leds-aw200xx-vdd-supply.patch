From 8adc69b31f2b6968ee0b73b4c2f83671fa3d03a8 Mon Sep 17 00:00:00 2001
From: sunshineinabox <aqemail@gmail.com>
Date: Mon, 1 Jun 2026 01:16:17 +0000
Subject: [PATCH] leds: aw200xx: enable an optional VDD supply

Gate the AW200xx VDD rail via an external regulator.
Enable an optional "vdd" supply during probe.
---
 drivers/leds/leds-aw200xx.c | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/drivers/leds/leds-aw200xx.c b/drivers/leds/leds-aw200xx.c
index b92158a..8533ffa 100644
--- a/drivers/leds/leds-aw200xx.c
+++ b/drivers/leds/leds-aw200xx.c
@@ -16,6 +16,7 @@
 #include <linux/module.h>
 #include <linux/mutex.h>
 #include <linux/regmap.h>
+#include <linux/regulator/consumer.h>
 #include <linux/time.h>
 #include <linux/units.h>
 
@@ -566,6 +567,11 @@ static int aw200xx_probe(struct i2c_client *client)
 	if (IS_ERR(chip->regmap))
 		return PTR_ERR(chip->regmap);
 
+	ret = devm_regulator_get_enable_optional(&client->dev, "vdd");
+	if (ret < 0 && ret != -ENODEV)
+		return dev_err_probe(&client->dev, ret,
+				     "Failed to enable vdd regulator\n");
+
 	chip->hwen = devm_gpiod_get_optional(&client->dev, "enable",
 					     GPIOD_OUT_HIGH);
 	if (IS_ERR(chip->hwen))
-- 
2.55.0
